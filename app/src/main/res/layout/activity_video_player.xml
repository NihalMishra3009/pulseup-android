<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ttt"
    tools:context=".videotutorial_player">

    <!-- CardView with Rounded Corners for VideoView -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="16dp"
        android:layout_gravity="center"
        app:cardCornerRadius="24dp"
        app:cardElevation="0dp">

        <VideoView
            android:id="@+id/videoView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:backgroundTint="@color/white"
            android:layout_gravity="center"
            android:adjustViewBounds="true" />
    </androidx.cardview.widget.CardView>

    <!-- Control Overlay -->
    <LinearLayout
        android:id="@+id/controlOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        android:background="#69000000">

        <!-- Buttons Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="24dp">

            <ImageButton
                android:id="@+id/backwardBtn"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:background="@android:color/transparent"
                android:src="@drawable/backward_button_white_icon"
                android:tint="@android:color/white"
                tools:ignore="UseAppTint" />

            <ImageButton
                android:id="@+id/playPauseBtn"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:background="@android:color/transparent"
                android:src="@drawable/pause_button_round_white_icon__1_"
                android:tint="@android:color/white"
                tools:ignore="UseAppTint" />

            <ImageButton
                android:id="@+id/forwardBtn"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:background="@android:color/transparent"
                android:src="@drawable/forward_button_white_icon"
                android:tint="@android:color/white"
                tools:ignore="UseAppTint" />
        </LinearLayout>

        <!-- SeekBar with Rotation Button and Speed Spinner -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="25dp"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:gravity="center_vertical">

            <SeekBar
                android:id="@+id/seekBar"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginTop="5dp"
                android:progressDrawable="@drawable/seekbar_progress"
                android:thumbTint="@android:color/white" />

            <ImageButton
                android:id="@+id/rotationBtn"
                android:layout_width="50dp"
                android:layout_height="50dp"
                android:layout_marginStart="12dp"
                android:background="@android:color/transparent"
                android:src="@drawable/aspect_ratio_icon"
                android:contentDescription="Toggle Rotation"
                android:tint="@android:color/white"
                tools:ignore="UseAppTint" />

            <!-- Speed Control Spinner -->
            <Spinner
                android:id="@+id/speedSpinner"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:entries="@array/speed_options"
                android:popupBackground="@android:color/white"
                android:backgroundTint="@android:color/white"
                android:padding="8dp" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>